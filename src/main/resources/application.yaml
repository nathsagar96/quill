spring:
  application:
    name: quill
  mvc:
    problemdetails:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate.jdbc.time_zone: UTC

quill:
  jwt:
    key-store: config/jwt-keys.json
    expiration: 24h
